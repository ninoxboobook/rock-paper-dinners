// Central place for UI icons (Phosphor). Emoji are still used for the slot-machine
// reels and cuisine pins — see lib/cuisine.ts — this is only for app chrome.
export {
  Cherries as PlayIcon,
  MapTrifold as MapIcon,
  ForkKnife as BrowseIcon,
  SlidersHorizontal as FilterIcon,
  Star as StarIcon,
  Vibrate as ShakeIcon,
  ArrowsClockwise as SpinIcon,
  NavigationArrow as DirectionsIcon,
  MapPin as MapsIcon,
  InstagramLogo as InstagramIcon,
  Confetti as ConfettiIcon,
  MagnifyingGlass as SearchIcon,
} from '@phosphor-icons/react'
